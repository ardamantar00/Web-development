@model AppUser
@{
    Layout = "_AdminLayout";
}
<div class="row">
    <div class="card">
        <div class="card-body">
            <div class="col text-center">
            <p>
                @Model.UserName isimli kategoriyi silmek istiyor musunuz ? 
            </p>
        <form asp-controller = "User" asp-action="DeleteConfirm">
            <input type="hidden" name="id" value="@Model.Id">
            <button type="submit" class="btn btn-danger">Sil</button>
            <a asp-controller="User" asp-action="Index" class="btn btn-primary">İptal</a>
        </form>
    </div>
        </div>
    </div>
    
</div>